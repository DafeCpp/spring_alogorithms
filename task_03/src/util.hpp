#include<map>
#include<vector>
#include<string>
using namespace std;

vector<string> get_komb_letters(const string& num)
map<int, char> button;
button[1] = {};
button[2] = {'a', 'b', 'c'};
button[3] = {'d', 'e', 'f'};
button[4] = {'g', 'h', 'i'};
button[5] = {'j', 'k', 'l'};
button[6] = {'m', 'n', 'o'};
button[7] = {'p', 'q', 'r', 's'};
button[8] = {'t', 'u', 'v'};
button[9] = {'w', 'x', 'y', 'z'};
button[0] = {'+'}

vector<string> res;

for(int i = 0; i < len(button[num[0]]); i++){
    for (int j = 0; j < len(button[num[1]]); j++){
        s = button[num[0]][i] + button[num[1]][j];
        res

    }
   
}



